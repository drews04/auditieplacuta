// Events NEW Badge JavaScript
// No JS strictly required for the badge; session is set server-side on index.
// This file exists for future enhancements (e.g., mark-as-seen via fetch).

document.addEventListener('DOMContentLoaded', function() {
    // Future enhancements can be added here
    // For example: mark events as seen via AJAX, real-time updates, etc.
    
    console.log('Events NEW badge system loaded');
});
